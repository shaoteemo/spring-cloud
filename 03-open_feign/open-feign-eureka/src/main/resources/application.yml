server:
  port: 80

spring:
  application:
    name: OpenFeignEurekaServer

#该演示在单机环境进行。需关闭Eureka服务注册发现
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:80